# タスク・保留事項一覧

最終更新: 2026-07-22

GitHub issue に記録されていない保留事項・タスクをここに残す。
issue 化したらこのファイルから削除し、issue へ移す。

---

## 1. 保留中の判断事項

### 1.1 厳密解スクリプト（exact_bb.py）をリポジトリに入れるか

- 分枝限定法による厳密最適解ソルバー。プレゼン資料 第7章の比較（達成率 87.4%）の根拠
- 現在はセッションの一時領域（スクラッチパッド）にあり、**リポジトリ未収録のため消失リスクあり**
- 検証ツール（`tools/` など）としてコミットするか、破棄するかは未決定
- 測定結果自体は `docs/presentation.md` 第7章に記録済み

---

## 2. 未着手タスク（issue 化候補）

優先順は 2.1 → 2.2 →（再測定 2.3）→ 2.4 が現実的
（時間軸拡張は係数と前処理化を先に解決しないと品質が悪化するため。資料 8.1 の注記参照）。

### 2.1 ペナルティ係数の適正化 ★最優先

- 現状 `A = 報酬上限 + 1 = 30.050` に対し目的項の差は 0.15〜2.4 で、1〜2桁の乖離
- エネルギー地形が制約充足に支配され、実行可能解どうしの優劣を識別できていない
  （達成率 87.4% の主因と推定。reads/sweeps を16倍にしても 17.400 止まり）
- 制約ごとに「違反を防ぐのに必要な最小値」を個別算出し、桁差を縮める
- ユーザー方針: 係数は一旦仮値のまま、後ほど考慮する（2026-07-22 合意）

### 2.2 前提科目の前処理化の機構実装（確定方針・実装issue予定）

- 要件: `docs/requirements.md` §3.5（確定方針）／設計論点: `docs/presentation.md` 4.2
- 機構候補3案の比較検討: バンドル化／変数消去（含意の構造化）／候補集合の制限
- 難所: 予算制約との結合（子を選ぶと親の hours も消費）、OR の選択自由度、親の共有
- 効果見込み（試算済み）: QUBO 53変数 → 34変数（−36%）、二次項 653 → 561（−14%）、
  前提違反サンプル（5000件中4168件 = 83%）が構造的に消える

### 2.3 前処理化後の再測定

- 2.2 実装後、厳密解との比較（現状 87.4%）を取り直す
- 実行可能率は改善見込みだが、予算制約側のペナルティ係数の桁差問題（2.1）は残る

### 2.4 時間軸拡張（z_{j,t}）の QUBO 規模見積もり

- 構想は `docs/presentation.md` 8.1（27項目 × 24週 = 648変数）
- 順序制約・週次上限制約を QUBO 化した場合の変数・二次項の増加量は**未見積もり**
- 見積もりが出るまで、発表では「実装可能」と言い切らない

### 2.5 係数の実データ化

- 分野プロファイル・シナジー値・最低相性 0.60 はすべて仮値
- 求人票の共起頻度などを根拠データとして差し替える（資料 8.2-3）

### 2.6 学習項目の拡充

- 現在39項目。分野別総時間が 100h〜630h と不均衡（資料 付録A.2）
- 粒度を揃えつつ項目数を増やす（2.4 の時間軸拡張と合わせて問題規模を上げる）

### 2.7 流行項（トレンド）の導入

- 人気度を線形項に組み込む。データ源・スコア化は未定（issue #1 コメント由来、資料 8.2-5）

### 2.8 実機 D-Wave QPU での実行

- サンプラー注入は実装済み（`recommend(..., sampler=...)`）
- 埋め込み（現状53変数・二次項653、前処理化後34変数・561）の検証が必要

---

## 3. 質疑リスク（保留・発表前に再確認）

プレゼン資料の付録・本文に記載済みだが、対応自体は保留のもの。

### 3.1 フロントエンドとモバイルアプリのプロファイルが完全同一

- `config/fields.py` の5軸ベクトルが一致しており、適性から2分野を原理的に区別できない
- 常に同じ相性スコアになる（デモでも両方 0.800）。実質9分野分の解像度
- 対応案: どちらかのプロファイルを差別化する（値の再設計が必要）

### 3.2 分野別総時間の不均衡（6倍差）

- ゲーム開発 630h vs グラフィックデザイン 100h
- 時間予算が少ないユーザーには重い分野が構造的に不利
- 対応案: 項目の粒度を揃える（2.6 と同根）

### 3.3 シナジーが項目価値より1桁小さい

- シナジー値域 −0.5〜+0.4 に対し項目価値 V は 0.65〜2.40
- 「二次項が QUBO の必然性」と主張する割に、目的関数への実効果が薄い
  （デモの目的関数 16.300 のうちシナジー寄与は +0.700 のみ）
- 対応案: 係数見直し（2.1・2.5）の際にスケールを再設計

### 3.4 「古典手法で十分では」への回答

- 現規模（2^27）では貪欲法・厳密解が速度・品質とも上回る（資料 第7章で誠実に開示済み）
- 回答の柱: 時間軸拡張（2.4）で古典手法が破綻する規模になる、という発展の見通し
- 2.4 の見積もりが未了のため、この回答の裏付けはまだ弱い点に注意
